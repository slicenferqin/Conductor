# Conductor UI/UX Design Document

## 1. Overview

Conductor 是一个多 Agent AI 协作平台，通过可视化界面让用户观察和参与 AI 团队的协作过程。

### 1.1 设计目标

- **透明可观察**：实时展示每个 Agent 的工作状态和进度
- **自然交互**：类似 Slack/Discord 的聊天式协作体验
- **简洁高效**：专注于核心功能，避免信息过载

### 1.2 技术栈

- **Frontend**: React 18 + TypeScript + TailwindCSS
- **State Management**: Zustand
- **Real-time**: WebSocket
- **Backend API**: FastAPI
- **UI Components**: shadcn/ui

---

## 2. Information Architecture

```
Conductor App
├── Projects List (项目列表)
│   ├── Active Projects
│   └── Completed Projects
│
├── Project Workspace (项目工作区)
│   ├── Header (项目信息)
│   ├── Team Panel (团队面板)
│   ├── Chat Area (协作聊天)
│   └── Files Panel (文件面板)
│
└── Settings (设置)
    ├── Agent Configuration
    └── Preferences
```

---

## 3. Core Layouts

### 3.1 Main Layout - Three Column

```
┌─────────────────────────────────────────────────────────────────────┐
│  🎼 Conductor                                    [Settings] [User]  │
├──────────┬──────────────────────────────────────────┬───────────────┤
│          │                                          │               │
│ PROJECTS │              CHAT AREA                   │    TEAM       │
│          │                                          │               │
│ ● Active │  ┌─────────────────────────────────┐    │ ┌───────────┐ │
│   proj-1 │  │ 🤖 秘书                         │    │ │ 🎯 产品经理│ │
│   proj-2 │  │ 团队已组建完成！                │    │ │ ● WORKING │ │
│          │  └─────────────────────────────────┘    │ └───────────┘ │
│ ○ Done   │                                          │               │
│   proj-3 │  ┌─────────────────────────────────┐    │ ┌───────────┐ │
│          │  │ 🎯 产品经理                     │    │ │ 🏗️ 架构师  │ │
│          │  │ 📖 读取文件: package.json       │    │ │ ○ IDLE    │ │
│          │  │ 正在分析需求...                 │    │ └───────────┘ │
│          │  └─────────────────────────────────┘    │               │
│          │                                          │ ┌───────────┐ │
│          │  ┌─────────────────────────────────┐    │ │ 💻 后端    │ │
│          │  │ User Input                       │    │ │ ○ WAITING │ │
│          │  │ ____________________________     │    │ └───────────┘ │
│          │  │ [Send]                           │    │               │
│          │  └─────────────────────────────────┘    │               │
│          │                                          │               │
├──────────┴──────────────────────────────────────────┴───────────────┤
│  Status: Project running • 3/5 agents working                       │
└─────────────────────────────────────────────────────────────────────┘
```

### 3.2 Responsive Breakpoints

| Breakpoint | Layout |
|------------|--------|
| Desktop (≥1280px) | Three columns |
| Tablet (768-1279px) | Two columns (Team as overlay) |
| Mobile (<768px) | Single column with tabs |

---

## 4. Component Design

### 4.1 Team Panel - Agent Cards

Agent 状态是核心信息，需要清晰可见。

#### Agent Status States

| Status | Icon | Color | Description |
|--------|------|-------|-------------|
| `ONLINE` | ● | Green (#22C55E) | 已连接，空闲等待 |
| `WORKING` | ◉ (animated) | Blue (#3B82F6) | 正在执行任务 |
| `WAITING` | ◐ | Yellow (#EAB308) | 等待其他 Agent |
| `OFFLINE` | ○ | Gray (#6B7280) | 已断开/未启动 |
| `ERROR` | ⚠ | Red (#EF4444) | 执行出错 |

#### Agent Card Component

```
┌─────────────────────────────────┐
│  🎯 产品经理                    │
│  ◉ WORKING                      │  ← 状态指示器 (带动画)
│  ─────────────────────          │
│  📖 读取 prd-template.md        │  ← 当前动作
│  ████████░░ 80%                 │  ← 进度条 (可选)
│                                 │
│  [@] [📄] [⚙️]                   │  ← 快捷操作
└─────────────────────────────────┘

[@] = @mention 此 Agent
[📄] = 查看此 Agent 产出的文件
[⚙️] = Agent 设置
```

#### Agent Card States

```tsx
// Idle State
┌─────────────────────────────────┐
│  🏗️ 架构师                      │
│  ○ IDLE                         │
│  ─────────────────────          │
│  等待任务分配                   │
└─────────────────────────────────┘

// Working State (with animation)
┌─────────────────────────────────┐
│  💻 后端开发          ◉ ← pulse │
│  ◉ WORKING                      │
│  ─────────────────────          │
│  📝 写入 backend/api/users.py   │
│  ████████████░░░░░░ 65%         │
└─────────────────────────────────┘

// Waiting State
┌─────────────────────────────────┐
│  🎨 前端开发                    │
│  ◐ WAITING                      │
│  ─────────────────────          │
│  等待 @架构师 完成 API 设计     │
└─────────────────────────────────┘

// Error State
┌─────────────────────────────────┐
│  🧪 测试工程师                  │
│  ⚠ ERROR                        │
│  ─────────────────────          │
│  chunk exceed limit             │
│  [重试] [查看日志]              │
└─────────────────────────────────┘
```

### 4.2 Chat Area - Message Bubbles

#### Message Types

1. **User Message** - 用户输入
2. **Agent Message** - Agent 正式输出
3. **Progress Message** - Agent 工作进度 (dimmed)
4. **System Message** - 系统通知

#### Message Bubble Design

```
┌─ User Message ──────────────────────────────────────┐
│                                                     │
│                          调研一下字节跳动这家公司   │
│                          做成可视化页面             │
│                                      👤 用户 14:30  │
└─────────────────────────────────────────────────────┘

┌─ Agent Message ─────────────────────────────────────┐
│ 🔍 调研员                                    14:32  │
│ ─────────────────────────────────────────────────   │
│ **调研任务已完成！**                                │
│                                                     │
│ 我已完成字节跳动公司的详细调研报告，主要发现：      │
│ 1. 公司规模：估值达5000亿美元...                    │
│ 2. 核心产品：抖音、TikTok...                        │
│                                                     │
│ 📎 docs/research.md                                 │
│                                                     │
│ @分析师 @前端开发 ← 可点击的 @mention              │
└─────────────────────────────────────────────────────┘

┌─ Progress Message (dimmed) ─────────────────────────┐
│ 🎯 产品经理 💭                              14:31   │
│ 📖 读取文件: requirements.md                        │
└─────────────────────────────────────────────────────┘

┌─ System Message ────────────────────────────────────┐
│         ─── 🤖 秘书组建了团队 (5 人) ───            │
└─────────────────────────────────────────────────────┘
```

#### @Mention Interaction

点击消息中的 `@角色名` 时：
1. 高亮右侧 Team Panel 中对应的 Agent Card
2. 显示该 Agent 的详细状态
3. 可选择发送消息给该 Agent

### 4.3 Project Header

```
┌─────────────────────────────────────────────────────────────────────┐
│  📋 调研字节跳动这家公司...                                          │
│  ─────────────────────────────────────────────────────────────────  │
│  Status: ● RUNNING    Duration: 00:05:32    Agents: 3/5 working     │
│                                                                     │
│  [⏸️ Pause] [⏹️ Stop] [📁 Open Workspace] [📊 View Progress]         │
└─────────────────────────────────────────────────────────────────────┘
```

### 4.4 Input Area

```
┌─────────────────────────────────────────────────────────────────────┐
│ ┌─────────────────────────────────────────────────────────────────┐ │
│ │ 输入需求或发送消息给团队...                                     │ │
│ │                                                           [@] [📎]│ │
│ └─────────────────────────────────────────────────────────────────┘ │
│                                                                     │
│ Quick actions: [@产品经理] [@架构师] [@后端] [@前端] [New Project]  │
└─────────────────────────────────────────────────────────────────────┘

[@] = 打开 @mention 选择器
[📎] = 附加文件
```

---

## 5. User Flows

### 5.1 Create New Project

```
User clicks "New Project"
        ↓
┌─────────────────────────┐
│ 🆕 New Project          │
│ ───────────────────     │
│ 输入你的需求：          │
│ ┌─────────────────────┐ │
│ │                     │ │
│ │                     │ │
│ └─────────────────────┘ │
│                         │
│ [Cancel]    [🚀 Start]  │
└─────────────────────────┘
        ↓
Secretary analyzes requirement
        ↓
Team formation animation
        ↓
Agents start working
```

### 5.2 @Mention an Agent

```
User types "@" in input
        ↓
┌─────────────────────────┐
│ Select agent to mention │
│ ───────────────────     │
│ 🎯 产品经理   ● IDLE    │
│ 🏗️ 架构师    ○ WORKING │
│ 💻 后端开发  ◐ WAITING │
│ ...                     │
└─────────────────────────┘
        ↓
User selects agent
        ↓
"@产品经理 " inserted in input
        ↓
User completes message and sends
        ↓
Agent receives notification and responds
```

### 5.3 View Agent Details

```
User clicks Agent Card
        ↓
┌─────────────────────────────────────┐
│ 🎯 产品经理                    [×]  │
│ ────────────────────────────────    │
│ Status: ● WORKING                   │
│ Current: 📖 读取 package.json       │
│ Duration: 00:02:15                  │
│                                     │
│ 📊 Activity Log                     │
│ ────────────────────────────────    │
│ 14:32:01 Started task               │
│ 14:32:05 📖 Read requirements.md    │
│ 14:32:08 📖 Read package.json       │
│ 14:32:15 📝 Writing prd.md          │
│                                     │
│ 📁 Files Created                    │
│ ────────────────────────────────    │
│ • docs/prd.md                       │
│                                     │
│ [📧 Send Message] [⏹️ Stop Agent]   │
└─────────────────────────────────────┘
```

---

## 6. Real-time Communication

### 6.1 WebSocket Events

```typescript
// Client -> Server
interface ClientEvent {
  type: 'create_project' | 'send_message' | 'mention_agent' | 'stop_project';
  payload: any;
}

// Server -> Client
interface ServerEvent {
  type:
    | 'project_created'
    | 'team_formed'
    | 'agent_status_changed'
    | 'agent_progress'
    | 'new_message'
    | 'project_completed'
    | 'error';
  payload: any;
}
```

### 6.2 Agent Status Sync

```typescript
// Agent status change event
interface AgentStatusEvent {
  type: 'agent_status_changed';
  payload: {
    project_id: string;
    agent_id: string;
    status: 'ONLINE' | 'WORKING' | 'WAITING' | 'OFFLINE' | 'ERROR';
    current_action?: string;  // e.g., "📖 读取 file.md"
    progress?: number;        // 0-100
    error_message?: string;
  };
}
```

### 6.3 Progress Updates

```typescript
// Real-time progress from agent
interface AgentProgressEvent {
  type: 'agent_progress';
  payload: {
    project_id: string;
    agent_id: string;
    action_type: 'read' | 'write' | 'bash' | 'search' | 'thinking';
    action_detail: string;  // e.g., "backend/api/users.py"
    timestamp: string;
  };
}
```

---

## 7. API Endpoints

### 7.1 REST API

```
# Projects
GET    /api/projects              # List all projects
POST   /api/projects              # Create new project
GET    /api/projects/:id          # Get project details
DELETE /api/projects/:id          # Delete project
POST   /api/projects/:id/stop     # Stop running project

# Messages
GET    /api/projects/:id/messages # Get project messages
POST   /api/projects/:id/messages # Send message to project

# Agents
GET    /api/projects/:id/agents   # Get project agents
GET    /api/projects/:id/agents/:agent_id  # Get agent details
POST   /api/projects/:id/agents/:agent_id/stop  # Stop agent

# Files
GET    /api/projects/:id/files    # List project files
GET    /api/projects/:id/files/*  # Get file content
```

### 7.2 WebSocket

```
WS /ws/projects/:id

# Connection provides real-time updates for:
# - Agent status changes
# - New messages
# - Progress updates
# - Project completion
```

---

## 8. Color Palette

### 8.1 Base Colors

| Name | Hex | Usage |
|------|-----|-------|
| Background | #0F172A | Main background |
| Surface | #1E293B | Cards, panels |
| Border | #334155 | Dividers |
| Text Primary | #F8FAFC | Main text |
| Text Secondary | #94A3B8 | Secondary text |
| Text Muted | #64748B | Hints, timestamps |

### 8.2 Status Colors

| Status | Hex | Usage |
|--------|-----|-------|
| Success/Online | #22C55E | Online, completed |
| Primary/Working | #3B82F6 | Working, active |
| Warning/Waiting | #EAB308 | Waiting, pending |
| Error | #EF4444 | Error, failed |
| Muted/Offline | #6B7280 | Offline, disabled |

### 8.3 Agent Colors (Emoji-based)

每个角色使用其 emoji 作为头像，无需额外颜色编码。

---

## 9. Animations

### 9.1 Status Indicator Pulse

```css
/* Working status pulse animation */
@keyframes pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.5; }
}

.status-working {
  animation: pulse 2s ease-in-out infinite;
}
```

### 9.2 Message Slide-in

```css
/* New message slide in from bottom */
@keyframes slideIn {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.message-new {
  animation: slideIn 0.3s ease-out;
}
```

### 9.3 Agent Progress Bar

```css
/* Progress bar fill animation */
@keyframes progressFill {
  from { width: 0%; }
  to { width: var(--progress); }
}

.progress-bar {
  animation: progressFill 0.5s ease-out forwards;
}
```

---

## 10. Implementation Phases

### Phase 1: Core UI (Week 1)
- [ ] Project layout with three columns
- [ ] Agent Card component with status
- [ ] Message bubble components
- [ ] Basic styling with TailwindCSS

### Phase 2: Backend API (Week 2)
- [ ] FastAPI endpoints for projects/messages
- [ ] WebSocket server for real-time updates
- [ ] Integration with existing Orchestrator

### Phase 3: Real-time Features (Week 3)
- [ ] WebSocket client in React
- [ ] Live agent status updates
- [ ] Progress streaming to UI

### Phase 4: Polish (Week 4)
- [ ] Animations and transitions
- [ ] Responsive design
- [ ] Error handling and edge cases
- [ ] Testing and bug fixes

---

## 11. File Structure

```
conductor/
├── frontend/                    # React frontend
│   ├── src/
│   │   ├── components/
│   │   │   ├── layout/
│   │   │   │   ├── MainLayout.tsx
│   │   │   │   ├── Header.tsx
│   │   │   │   └── Sidebar.tsx
│   │   │   ├── project/
│   │   │   │   ├── ProjectList.tsx
│   │   │   │   ├── ProjectHeader.tsx
│   │   │   │   └── ProjectWorkspace.tsx
│   │   │   ├── team/
│   │   │   │   ├── TeamPanel.tsx
│   │   │   │   ├── AgentCard.tsx
│   │   │   │   └── AgentDetailModal.tsx
│   │   │   ├── chat/
│   │   │   │   ├── ChatArea.tsx
│   │   │   │   ├── MessageBubble.tsx
│   │   │   │   ├── InputArea.tsx
│   │   │   │   └── MentionSelector.tsx
│   │   │   └── ui/              # shadcn/ui components
│   │   ├── hooks/
│   │   │   ├── useWebSocket.ts
│   │   │   ├── useProject.ts
│   │   │   └── useAgents.ts
│   │   ├── stores/
│   │   │   ├── projectStore.ts
│   │   │   └── agentStore.ts
│   │   ├── types/
│   │   │   └── index.ts
│   │   └── App.tsx
│   └── package.json
│
├── conductor/
│   └── server/                  # FastAPI backend
│       ├── main.py
│       ├── routes/
│       │   ├── projects.py
│       │   ├── messages.py
│       │   └── agents.py
│       ├── websocket/
│       │   └── handler.py
│       └── schemas/
│           └── events.py
```

---

## 12. Summary

本设计文档定义了 Conductor 可视化界面的核心功能：

1. **三栏布局**：项目列表 | 聊天区 | 团队面板
2. **Agent 状态系统**：5 种状态（Online/Working/Waiting/Offline/Error）
3. **实时通信**：WebSocket 推送状态变更和消息
4. **@Mention 交互**：点击触发 Agent 响应

下一步：按 Phase 1 开始实现核心 UI 组件。